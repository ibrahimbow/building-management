server:
  port: 8081

spring:
  application:
    name: bm-auth-service

  datasource:
    url: jdbc:postgresql://localhost:5432/building_management_db
    username: postgres
    password: admin123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

security:
  jwt:
    secret: "local-dev-secret-key-for-building-management-auth-service-please-change-in-production-2026"
    expiration-minutes: 60